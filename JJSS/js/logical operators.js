// logical operators
// && AND
// || OR
// ! NOT
// &&  AND:- need both side must be true to give you true
// ||  Or:- one side must be true
// !  NOT:- return the boolean opposite 
 // not true = false 
 // !(12*2==24 && 4-6<0)
 // !(24==24 && -2<0) --> !(T&&T)=false
 // here the computer its gonna read if statment first
 // if its true its gonna stop if not its gonna go 
 // to the else if statment if not go to else and print it
 // and here i can have as many i want else if statment between the if --> and the else
 

 if("evan" == "evan" && "matta" =="matta") {
 	console.log("if statment ran")
 }
 else if (8>7 || 6<2 ) {
 	console.log("if else statment ran")
 }
 else{
 	console.log("else statment ran")
 }